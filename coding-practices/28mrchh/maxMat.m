% Find the maximum value in the given matrix
% For example, if
%   A = (1 2 3; 4 7 8; 9 1);
% then the answer is 9
% Your function name should be MaxMat.

function y=maxMat(x)

y=max(max(x));  % her blokta olani buluyor matrixte 

end